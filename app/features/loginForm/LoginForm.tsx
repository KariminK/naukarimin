import { Form } from "react-router";
import { TextInput } from "./components";

const LoginForm = () => {
  return (
    <Form>
      <TextInput></TextInput>
    </Form>
  );
};

export default LoginForm;
